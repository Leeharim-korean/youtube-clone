extends layouts/main
include mixins/videoBlock

block content
    .videos
        each video in videos 
            +videoBlock({
                id:video.id,
                title:video.title,
                views:video.views,
                videoFile:video.videoFile
            })
//- each Variable in ArrayName(iteration):you can use each elements of array